# Аудит pinguin на соответствие правилам ботов VK — 2026-08-31

## Вывод
Код соответствует правилам. Нарушений, требующих правок, нет; изменения кода не планируются.

## Проверка по пунктам
- **П.1–1.2, 3 (подписки/лайки/репосты как условие)** — не нарушено: текстов-просьб нет (приветствие, прощание, статусы — main.go:493–523, worker.go:128); функциональность не зависит от членства.
- **П.2, 2.2 (выполнение ≤24 ч, информирование)** — не нарушено: подписка на IP мгновенная (bhAnyWithMatch), пауза помечается `⏸️` в статусе.
- **П.4 (ясность команд)** — не нарушено: `/stop`, `/restart`, кнопки-эмодзи с очевидной семантикой и снекбар-подтверждением (vk.go:149).
- **П.5, 6, 7.1 (внешние сайты, реклама, рассылки в беседы)** — не нарушено: рекламы, ссылок и рассылок нет; только ответы на запросы.
- **П.8 (источник пользовательского контента)** — не нарушено: статусы уходят reply/forward на запрос (vk.go:100, sendStatusReply).
- **Монетизация** — неприменимо: монетизации нет.

## Организационные замечания (вне кода)
1. П.2.1: убедиться, что в сообществе бота на видном месте есть информация о технических сбоях.
2. `bhReplyMessageIsMinus` (main.go:419) удаляет любое сообщение по ответу «-», включая чужие; требуется админ-право бота во всех чатах из `pinguin_ids` (иначе VK вернёт ошибку — код её обрабатывает).
